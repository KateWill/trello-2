<h1><%= @board.name %></h1>
<br>
<%= link_to "Add List", new_board_list_path(@board) %>
<br>
<ul>
<% @lists.each do |list| %>
  <li><%=link_to list.name, board_list_path(@board, list.id) %>
  <% end %>
</ul>
